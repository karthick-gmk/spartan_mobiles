from django.core.management.base import BaseCommand\nfrom spartans.models import Product, Brand, Category\n\nclass Command(BaseCommand):\n    help = 'Create sample data'\n\n    def handle(self, *args, **options):\n        # Create sample brand and category\n        brand, _ = Brand.objects.get_or_create(name='Sample Brand')\n        category, _ = Category.objects.get_or_create(name='Mobile')\n        \n        # Create sample product\n        Product.objects.get_or_create(\n            name='Sample Phone',\n            defaults={\n                'brand': brand,\n                'category': category,\n                'price': 25000,\n                'description': 'Sample mobile phone',\n                'stock': 10\n            }\n        )\n        \n        self.stdout.write('Sample data created successfully')